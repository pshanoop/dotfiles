color status black blue

# Set the folder so that record, etc are all relative.
set folder    = "~/.local/share/maildir/minimalistech/"
set record    = "+INBOX"
set postponed = "+Drafts"

set from = "hugo@minimalistech.com"
