# Restart offlineimap on an hourly basis since it tends to stall every
# once in a while.
0	*	*	*	*	chronic vdirsyncer sync
15      5       *       *       *       /bin/sh /home/hugo/.config/cron/scripts/backup-mailbox.sh
35      5       *       *       *       /bin/sh /home/hugo/.config/cron/scripts/backup-photos.sh
50      5       *       *       *       /bin/sh /home/hugo/.config/cron/scripts/backup-bit-wallet.sh
# Compact mailbox index weekly
0	0	*	*	1	chronic notmuch compact
