let b:ale_fixers = ['gofmt']

setlocal shiftwidth=8  " When indenting, go 8 spaces at a time.
setlocal tabstop=8     " A tab is equal to 8 spaces.
setlocal noexpandtab   " Don't expand tabs into spaces (go uses tabs).
