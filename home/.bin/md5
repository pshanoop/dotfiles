#!/usr/bin/env python3

import hashlib
import sys

if (len(sys.argv) <= 1):
    print("Usage: md5 STRING...")
    sys.exit(0)

m = hashlib.md5()
args = " ".join(sys.argv[1:])
m.update(args.encode("UTF-8"))
print(m.hexdigest())
