#!/bin/sh

set -x

# Never run more than one i3lock instance
pgrep i3lock && exit

systemctl --user stop caffeine &
killall -SIGUSR1 dunst &
xset dpms force off
xset dpms 3

systemctl --user start i3lock

xset dpms 120
killall -SIGUSR2 dunst
systemctl --user start caffeine
