if [ -f .ignore ]
then
	svn propset svn:ignore    -F .ignore . -q
	svn add .ignore -q --force
fi
if [ -f .externals ]
then
	svn propset svn:externals -F .externals . -q
	svn add .externals -q --force
fi
