#!/bin/sh

# The tun0 device needs to be created first:
#  sudo ip tuntap add dev tun0 mode tun user hugo

sudo openconnect secure.assia-inc.com -u hbarrera --passwd-on-stdin -i tun0 -U hugo -v < ~/priv/secrets/assia-vpn
